{
  "swagger": "2.0",
  "info": {
    "version": "1.0.0",
    "title": "Xerox-Feedback Swagger"
  },
  "host": "localhost:8080",
  "basePath": "/api",
  "tags": [
    {
      "name": "Feedback",
      "description": "API for Feedback in the system"
    }
  ],
  "schemes": [
    "http"
  ],
  "consumes": [
    "application/json"
  ],
  "produces": [
    "application/json"
  ],
  "paths": {
    "/feedback": {
      "post": {
        "tags": [
          "Feedback"
        ],
        "summary": "Create a New Feedback in the system",
        "description": "Create new Feedback in system",
        "parameters": [
          {
            "name": "Feedback",
            "in": "body",
            "description": "Feedback that we want to create",
            "schema": {
              "$ref": "#/definitions/Feedback"
            }
          }
        ],
        "produces": [
          "application/json"
        ],
        "responses": {
          "200": {
            "description": "New Feedback is created",
            "schema": {
              "$ref": "#/definitions/Feedback"
            }
          }
        }
      },
      "get": {
        "tags": [
          "Feedback"
        ],
        "summary": "Get all Feedbacks in system",
        "responses": {
          "200": {
            "description": "OK",
            "schema": {
              "$ref": "#/definitions/Feedback"
            }
          }
        }
      }
    }
   
  },
  "definitions": {
    "Feedback": {
      "required": [
        "comments",
        "rating"
      ],
      "properties": {
        "rating": {
          "type": "number",
          "uniqueItems": false
        },
        "comments": {
          "type": "string",
          "uniqueItems": false
        }
      }
    }
  }
}